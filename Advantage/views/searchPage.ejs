<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Advantage</title>
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <style>
        * {
            margin: 0px;
            padding: 0px;
        }

        .products {
            border: 1px solid #c3c4c5;
            flex-basis: 384px;
            /* height: 50vh;
            width: 25vw; */
            /*background-color: #D9D9D9;*/
            margin: 20px;
            border-radius: 10px;
            text-align: center;
        }

        .products:hover {
            cursor: pointer;
            box-shadow: 1px 1px 5px rgb(225, 243, 255);
        }

        .images {
            flex-basis: 384px;
            height: 50vh;
            width: 25vw;
            /*background-color: #D9D9D9;*/
            border-radius: 10px;
        }

        main {
            color: white;
        }

        #filters {
            display: inline-block;
            font-size: 2.5vw;
            margin-top: 2.2135vh;
            margin-left: 0.7320644vw;
            /* letter-spacing: 0.1vw; */
        }

        .fog {
            height: 41.958333vh;
            width: 21.40409vw;
            background-color: #c3c4c5;
            filter: blur(200px);
            opacity: 60%;
            border-radius: 50%;
            position: fixed;

        }

        #fog2 {
            left: -20%;
            bottom: 0%;
        }

        #fog1 {
            right: -10%;
        }

        #up {
            border-bottom: 1px solid #c3c4c5;
            /* background-color: blue; */
        }

        #bottom {
            display: flex;
        }

        #filterContainer {
            position: relative;
            /* background-color: purple; */
            width: 18.35vw;
            /* height: 100vh; */
        }

        #priceRange {
            display: block;
            /* margin-top: 2.3vh; */
            margin-left: 0.54vw;
            position: relative;
            top: 7px;
            font-size: 1.6vw;
        }

        #filterContainer input {
            display: inline-block;
            width: 3.6603221083455344070278184480234vw;
            background-color: #D9D9D9;
            margin-top: 1vh;
            font-weight: 500;
            padding: 3px;
            border-radius: 2px;
            border: 0px;
        }

        #filterContainer input::placeholder {
            opacity: 0.6;
        }

        #filterApply {
            padding: 6px 15px;
            font-size: small;
            background-color: #48D77D;
            border: 0px;
            border-radius: 2px;
            align-self: flex-end;
            transform: translateY(6%);
        }

        #filterApply:hover {
            background-color: #38a560;
        }

        #mainSection {
            /* background-color: aqua; */
            width: 96.339677891654465592972181551977vw;
            min-height: 80.1vh;
            display: flex;
            flex-wrap: wrap;
            /* justify-content: center; */
            border-left: 1px solid #c3c4c5;
        }

        #minAndMax {
            display: flex;
            margin-top: 2vh;
            justify-content: space-around;
            /* background-color: yellow; */
            align-items: flex-start;
        }
        #mainSection a{
            color: white;
            text-decoration: none;
        }
    </style>
</head>

<body>
    <%- include("Header.ejs") %>
        <main>
            <div id="up">
                <b id="filters">FILTERS</b>
            </div>
            <div class="fog" id="fog1"></div>
            <div class="fog" id="fog2"></div>
            <div id="bottom">
                <div id="filterContainer">
                    <b id="priceRange">Price Range</b>
                    <div id="minAndMax">
                        <input type="text" placeholder="min">
                        <input type="text" placeholder="max">
                        <button id="filterApply"><b>Apply</b></button>
                    </div>
                </div>
                <div id="mainSection">
                    <% for(let i=0;i < products.length; i++) { %>
                        <a href="http://localhost:3000/buyer/searchPage/product/<%= products[i].ProductId %>">
                            <div class="products">
                                <img src=<%=products[i].Image1Src %> alt="image" class="images">
                                <h4 style="margin-left: 20px;">
                                    <%= products[i].Name %>
                                </h4>
                                <h3 style="margin-left: 20px;"> Rs. <%= products[i].Price %>/- </h3>
                            </div>
                        </a>
                        <% }%>
                </div>
            </div>
        </main>
</body>

</html>